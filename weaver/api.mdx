---
title: "API conduits"
description: "Nexus and Appwrite API registry"
---

> Jurisdiction: Universal Translator Registry
> Version: V666.TOTAL_PARITY
> Protocol: Verdigris V12.0

This registry totalizes all high-privileged API conduits connecting the **Weaver Studio** to the **Nexus Kernel**.

## 📡 Base Endpoints

- **Nexus Engine**: `http://localhost:8001/api/v1`
- **Appwrite Administration**: `https://tor.cloud.appwrite.io/v1`

---

## 1. Orchestration Jurisdiction (`/orchestration`)

### `POST /weave`

Engages the **Council of Minds** with a full spatial graph specification.

- **Payload**: `{ spec: object, thread_id: string, input_files: object }`
- **Ritual**: Routes through **AEGIS** sentinel for constitutional audit before materializing the **CrewAI** swarm.

### `POST /generate-blueprint`

Mobilizes the **BEEP Oracle** to scaffold a spatial topology from natural language decrees.

---

## 🧠 2. Cognitive Plane (`/agentfs`)

### `GET /agentfs/content/{session_id}`

Scries the isolated sandbox VFS for high-fidelity artifact metadata (Merged View).

### `POST /agentfs/sync/{session_id}/pull`

Restores local parity with the cloud hippocampal bedrock.

### `POST /agentfs/sync/{session_id}/checkpoint`

Compacts the local WAL to bound disk usage while preserving sync state.

---

## 🧱 3. Administrative Plane (`/decrees` | `/functions` | `/databases`)

### `GET /decrees/databases`

Retrieves all registered TablesDB relational bedrock substrates.

### `POST /decrees/functions`

Materializes a new serverless logic filament in the enclave.

---

## 🛡️ 4. Identity & Governance (`/account` | `/management`)

### `GET /account`

Scries the identity signature of the currently synapsed architect.

### `GET /management/architects`

Scries the population of all registered signatures in the syndicate.

---

_Φ_VALIDATED: All conduits are synapsed with the Sovereign's Sigil._
